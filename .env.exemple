# Mongo root user created by the official entrypoint (for internal admin use)
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=change-me-root

# App-level users are created by mongo/initdb/01-users.js at first start
# Connection string used by migrator (ingestor user has readWrite + dbAdmin on meddb)
MONGO_URI=mongodb://ingestor:ingestorpass@mongodb:27017/meddb?authSource=admin

# Path to your CSV inside the container (the host path ./data is mounted at /app/data)
DATA_FILE=/app/data/raw/healthcare.csv